<div class="mincon" style="text-align: left;">

    <script>
        var srvprojs = '<%= projs %>'.split(',');
    </script>

    <div class="bx-switch row no-gutters">
        <span class="bx-opt active" id="usr-view-btn">
            <span class="box col-12">User View</span>
        </span>
        <div class="seperator"></div>
        <span class="bx-opt" id="proj-view-btn">
            <span class="box col-12 active">Project View</span>
        </span>
    </div>
    <div class="col-12 text-center">
        <button id="flush-cache-btn" class="btn btn-outline-danger col-4 col-offset-4 inactive flush-cache">Refresh Cache</button>
    </div><br/>

    <form id="graph-params-form" class="col-12 row no-gutters" action="/analytics" method=GET> <!-- will be serialized through ajax, then load the timesheet tables through ajax + d3. -->
        <!--div id="list1" class="col-3 dropdown-check-list" tabindex=1000>
            <span class="anchor">Select Users</span>
            <ul class="items">
                <li><input class="check" type="checkbox" />Will be</li>
                <li><input class="check" type="checkbox" />Filled with</li>
                <li><input class="check" type="checkbox" />Ajax (Later)</li>
            </ul>
        </div--> <!-- deprecated, sorry past me. -->
        <input id="search-type-val" name="searchtype" type="text" hidden />
        <select id="user-list" name="user" class="col-3 form-control" multiple>
        </select>
        <!--select id="fieldSelect" hidden value="Date">Date-->
            <!-- Okay, so like, i know this looks random, and if you're trying to understand my code, this is the worst hack, so strap in.
                    Basically, I had to hard code some stuff into the calendar-extender.js function, that would make it so that the calendar would only open if this was set to "Date" or something of the similar
                    It's actually some great functionality on the planner page. But, if you port it elsewhere, of course it can't find "#fieldSelect".
                    So I had three options, really, either I use a whole nother instance of that script just for the planner page (and another for this one etc)
                    Or, I come up with a complex system to tell if it should read in field select, such as an additional class to the calendar extender or something. But I'm tired rn and dont give a f***
                    Or, I do this, hold a random hidden fieldSelect that is always set to date. It's stupid, but it's a good hack for right now. So if future me / you are trying to move the calendar around, 
                    make sure to move around the hidden fieldSelect as well. -->
            <!-- Nevermind lol, it didnt work, then i realised that I could just check whether or not the value was null -->
        <!--/select-->
        <div class="col-3">
            <input name="fromdate" type="date" id="from-date" class="col-12" placeholder="Start date" required/>
        </div>
        <div class="col-3">
            <input name="todate" type="date" id="to-date" class="col-12" placeholder="End date" required/>
        </div>
        <div class="col-3">
            <button id="submit-btn" class="col-12 btn btn-outline-info">search</button>
        </div>
    </form>
    
    <div id="tables" class="col-12"></div>
    <div id="tooltip"></div>


    <!-- MOJS -->
    <script src="./lib/mo.js" crossorigin="anonymous"></script>
    <!-- D3 -->
    <script src="https://d3js.org/d3.v5.min.js"></script>
    <!-- CUSTOM -->
    <link rel="stylesheet" href="./calendar-extender.css"></link>
    <link rel="stylesheet" href="./analytics.css"></link>
    <script src="./burstpack.anim.js"></script>
    <script src="./analytics.js"></script>
    <script src="./calendar-extender.js"></script>
</div>